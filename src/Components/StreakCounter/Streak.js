import React from "react";
import { Square } from "@chakra-ui/react";

// const StreakCounter = (dateArr) => {
//   dateArr.map((item) => {
//     return (
//       <Square size="40px" bg="purple.700" color="white">
//         item
//       </Square>
//     );
//   });
// };

const streakCounter = (dateArr) => {
  dateArr.map((item) => {
    console.log(item);
  });
};

export default streakCounter;
